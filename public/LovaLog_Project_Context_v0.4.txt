LovaLog — Project Context & Version Log
========================================

1. Version
----------
v0.4 — 2026-02-08

2. Product Vision & Emotional Thesis
-------------------------------------
LovaLog is a power-user companion for Lovable developers. It sits inside the browser as a Chrome extension side-panel and also lives as a standalone website dashboard.

Emotional target: "Mission control for my build session."
- Fast, focused, zero-friction task capture
- Dark, dense, tool-like — closer to a terminal or IDE panel than a consumer app
- The user is a solo builder or small-team dev who thinks in features, not tickets
- Calm confidence: the UI should feel like it already knows what you need

User archetype: Power user who builds with Lovable daily. They want to:
- Capture feature ideas mid-flow without leaving Lovable
- Generate scoped prompts from feature descriptions + optional screenshots
- Inject those prompts directly into Lovable's chat
- Track what's next, what's in the backlog, and what's done

3. Current Feature Set
----------------------
EXISTS TODAY:
- Chrome extension (Manifest V3, side-panel)
  - OAuth login via chrome.identity (Google provider through Supabase)
  - Project CRUD (create, select, rename — stored in Supabase)
  - Feature/task CRUD per project
    - Title, description, optional image upload (Supabase Storage)
    - Status cycling: backlog → next → done
    - Manual drag-to-reorder (persisted `order` field)
  - AI prompt generation (edge function → Lovable AI Gateway / Gemini Flash)
  - Prompt injection into Lovable chat via content script
  - Free-credits scraping from Lovable UI via content script
  - Feature detail sheet (view/edit description, prompt, image, status)

- Website dashboard (same React app, different context)
  - Auth via Supabase email/password (standard login/signup)
  - Same project + feature CRUD
  - Same prompt generation
  - Extension-only controls hidden (inject buttons, credits badge)
  - Subtle "Install the extension to inject prompts" CTA

- Landing page at root route (website only; extension skips to auth)

- Shared sorting logic (v0.4):
  - Active section: status priority (next > backlog) → manual order ASC → updated_at DESC → created_at ASC
  - Completed section: updated_at DESC → created_at DESC

PLANNED NEXT:
- DB migration: UPDATE features SET status = 'next' WHERE status = 'in-progress'
- Prompt versioning / history
- Multi-image support per feature
- Potential: completed_at timestamp column

INTENTIONALLY DEFERRED:
- Team / collaboration features
- Billing / premium tier
- Mobile-native app
- Notification system
- Analytics / build-session tracking

4. Architectural Decisions
--------------------------
FRONTEND:
- React 18 + Vite + TypeScript + Tailwind CSS + shadcn/ui
- Why: Lovable's native stack; zero config friction
- MemoryRouter (not BrowserRouter): required for Chrome extension side-panel (no real URL bar)
  - Constraint: all routing must work without browser history API
  - Open question: could use BrowserRouter for website context only
- State: React Query for server state, local useState for UI state
- No global state manager (Redux, Zustand) — intentionally simple

BACKEND:
- Supabase (external project, NOT Lovable Cloud's auto-provisioned instance)
  - Why: project predates Lovable Cloud; migrating would break extension OAuth config
  - Constraint: src/integrations/supabase/client.ts uses hardcoded URL + anon key pointing to the external project
  - INVARIANT: Do not let Lovable Cloud overwrite this file (it's in the codebase but manually maintained)
- Edge function: generate-feature-prompt
  - Calls Lovable AI Gateway (ai.gateway.lovable.dev) with LOVABLE_API_KEY
  - Model: google/gemini-2.5-flash (good balance of speed + quality for prompt generation)
  - Accepts: title, description, image_url
  - Returns: generated prompt text

STORAGE:
- Supabase Storage bucket for feature screenshots/images
- Images uploaded from extension, viewable on both extension + website
- Public URLs used in prompts sent to AI

AUTH:
- Two parallel auth flows (same Supabase project):
  1. Extension: chrome.identity OAuth → Google → Supabase signInWithIdToken
  2. Website: email/password via Supabase signUp / signInWithPassword
- AuthProvider wraps the app; ProtectedRoute guards /dashboard
- Session persisted by Supabase client (localStorage)

CHROME EXTENSION:
- Manifest V3, side_panel API
- content.js: injected into Lovable tabs
  - Scrapes free credits from DOM (brittle: depends on specific text patterns)
  - Injects prompts by finding [aria-label="Chat input"] and simulating input
  - INVARIANT: these DOM selectors WILL break if Lovable changes their UI
- background.js: handles chrome.runtime messages between side-panel and content script
- useChromeMessaging hook: abstracts extension messaging for React components

5. Integration Boundaries
--------------------------
EXTENSION ↔ LOVABLE TAB:
- content.js runs in Lovable's page context
- Communication: chrome.runtime.sendMessage / onMessage
- Injection: finds chat input, sets value, dispatches input event, clicks send
- Credits: queries DOM for credit display text, parses number
- Risk: any Lovable UI change can break both features silently

EXTENSION ↔ BACKEND:
- Direct Supabase client calls (same as website)
- Edge function called via supabase.functions.invoke()
- Auth token attached automatically by Supabase client

WEBSITE ↔ BACKEND:
- Identical to extension (same client, same queries)
- No extension APIs available — isExtension check prevents calls

KNOWN PLATFORM LIMITATIONS:
- Chrome extensions cannot use BrowserRouter (no URL bar in side panel)
- Content scripts run in isolated world; must use message passing
- Manifest V3 service workers are ephemeral (no persistent background)
- Supabase anon key is exposed client-side (RLS policies are the security boundary)

6. Design Direction
-------------------
UI TONE:
- Dark theme only (no light mode planned)
- Compact, dense information display
- Purple accent (brand-purple) as the primary action color
- Muted grays for secondary content
- Green for "done" / success states
- Minimal animation; functional transitions only

LAYOUT PRINCIPLES:
- Extension: single-column, scrollable, fits 400px side panel
- Website: same component structure, slightly more breathing room
- Feature list is the primary view; everything else is secondary
- Detail sheet (slide-over) for editing — not a separate page
- No modals for CRUD; inline editing and sheets preferred

HERO / MARKETING:
- Landing page exists with cinematic hero image
- "Your Lovable Copilot" positioning
- Dark, dramatic, tool-focused aesthetic
- CTA drives to Chrome Web Store + login

7. Known Constraints, Risks & Sharp Edges
------------------------------------------
BRITTLE DOM SELECTORS:
- content.js depends on Lovable's DOM structure for injection and credit scraping
- These WILL break without warning when Lovable updates their UI
- No fallback mechanism currently exists

STATUS MIGRATION DEBT:
- Database may still contain 'in-progress' status values
- Sorting logic handles this (maps to same priority as 'next')
- But StatusBadge and other UI may not render correctly for old values
- Need to run: UPDATE features SET status = 'next' WHERE status = 'in-progress'

HARDCODED SUPABASE CLIENT:
- client.ts points to external Supabase, not Lovable Cloud
- If Lovable Cloud auto-regenerates this file, it will break the app
- Must be manually maintained

EDGE FUNCTION SECURITY:
- generate-feature-prompt may not validate JWT properly
- Anyone with the anon key could potentially call it
- Should add auth check: const { data: { user } } = await supabaseClient.auth.getUser()

NO COMPLETED_AT COLUMN:
- "Completed" sorting uses updated_at as proxy
- Any edit to a completed feature changes its position in the list
- Adding completed_at would fix this but requires migration + code changes

MEMORYROUTER LIMITATION:
- Deep links don't work (no URL to share)
- Browser back/forward buttons don't work in extension
- Acceptable for extension; may want BrowserRouter for website

8. What Changed in v0.4
------------------------
NEW DECISIONS:
- Status value 'in-progress' renamed to 'next' in type system
- Shared sorting logic extracted to src/lib/featureSorting.ts
- isExtension prop threading for conditional UI rendering
- Website hides: CreditsBadge, all Inject buttons (quick + detail sheet)
- Subtle "Install extension" CTA added to feature detail on web

INVALIDATED ASSUMPTIONS:
- Previously assumed single flat list with simple order sort
- Now: two sections (Active / Completed) with distinct sorting rules

SCOPE SHIFTS:
- Sorting is no longer just "order ASC" — it's a multi-key sort with status priority
- Website is now a first-class surface (not just "extension without inject")

9. Open Questions & Upcoming Decisions
---------------------------------------
1. DB MIGRATION: Run UPDATE features SET status = 'next' WHERE status = 'in-progress'?
   - Low risk, high value. Should do it.

2. EDGE FUNCTION AUTH: Lock down generate-feature-prompt with JWT validation?
   - Currently open to any request with anon key.

3. COMPLETED_AT COLUMN: Add a dedicated timestamp for completion?
   - Would fix sorting stability for completed features.

4. BROWSERROUTER FOR WEBSITE: Use BrowserRouter when !isExtension?
   - Would enable deep links, SEO, proper browser navigation on web.

5. CONTENT SCRIPT RESILIENCE: Add fallback selectors or detection for Lovable UI changes?
   - Currently single-point-of-failure DOM queries.

6. PROMPT VERSIONING: Store prompt history per feature?
   - Users regenerate prompts; previous versions are lost.

7. MULTI-DEVICE SYNC: Handle conflicts when same user edits from extension + website simultaneously?
   - Currently last-write-wins via Supabase.

---
End of Project Context Log
Generated: 2026-02-08
